# ─────────────────────────────────────────────────────────────
# PRIVACY GUARD — this repo is PUBLIC and GitHub-Pages-published.
# Anything matched here stays local and never reaches GitHub.
# Added 2026-05-31 after the public-repo leak risk flagged in the
# 29 May mission report (PII + n8n webhook token + promo codes).
# ─────────────────────────────────────────────────────────────

# Applicant PII (names live in here)
claude-desktop-context.md

# Internal strategy / handover docs — funnel numbers, tactics, decisions.
# Untracked 2026-06-05 (D-0265 structural fix). History not scrubbed by choice.
sessions/
SITREP.md
# Backups der internen Dateien. Der exakte Name oben deckt sie NICHT ab, und eine
# SITREP-Kopie enthaelt dieselbe interne Lage wie das Original. 2026-08-08.
SITREP.md.*
*.bak
*.bak-*
STRATEGY.md
CLAUDE.md
TODO.md
geo-audit-*.md
nationality-text-changes-proposal.md

# Routine config — holds the n8n webhook token
routines/

# Internal outreach: leads, SURF templates, community tracker,
# and the promo-code / link library (financial codes — must stay private)
outreach/

# Internal market/demographics research tied to private strategy (keep local)
research/

# Internal translation working files + preview screenshots (served pages live in docs/)
translation/

# macOS noise
.DS_Store
**/.DS_Store
